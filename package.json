{
  "name": "smith-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "test:api": "node scripts/apiSmokeTest.js",
    "test:unit": "npm test --prefix server",
    "test:ci": "npm run test:unit && npm run test:api",
    "test:e2e": "echo 'No E2E Playwright specs configured, skipping E2E'",
    "dev:clean": "node scripts/ensurePortFree.js && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
